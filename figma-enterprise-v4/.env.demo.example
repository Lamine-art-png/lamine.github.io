# AGRO-AI dedicated demo portal frontend
# Build the exact same portal commit as production, pointed at the demo API.
VITE_API_BASE_URL=https://demo-api.agroai-pilot.com

# Keep all secrets out of Vite. VITE_* values are browser-visible by design.
# Do NOT place provisioning tokens, demo passwords, Stripe secret keys,
# connector credentials, or allowlisted founder emails in this file.
