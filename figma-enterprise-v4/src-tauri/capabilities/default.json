{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum desktop capability set for the AGRO-AI Enterprise Portal main window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "http:default",
      "allow": [
        { "url": "https://api.agroai-pilot.com/**" }
      ]
    }
  ]
}
