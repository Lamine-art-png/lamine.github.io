# Terris preference data

Preference rows must be expert reviewed before DPO. Each JSONL row contains `prompt`, `chosen`, and `rejected`, plus provenance fields. Do not place customer-derived content here unless explicit training consent is recorded and de-identification has been completed.
