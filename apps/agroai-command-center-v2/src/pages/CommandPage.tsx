import { actions, SCENARIO_OPTIONS, useCommandStore } from "../state/commandStore";
import { Card, Metric } from "../components/PagePrimitives";
export function CommandPage() { const state = useCommandStore((s) => s); return <main className="page"><section className="hero"><p className="eyebrow">Command</p><h1>{state.decision.action}</h1><p>{state.decision.driver}</p><div className="actions"><button onClick={() => void actions.refreshIntelligence()}>Refresh intelligence</button><button onClick={() => actions.openDrawer()}>Upload records</button></div></section><div className="grid"><Card title="Decision"><Metric label="Block" value={state.decision.block}/><Metric label="Start" value={state.decision.start}/><Metric label="Confidence" value={state.decision.confidence}/></Card><Card title="Scenarios"><select value={state.scenarioId} onChange={(e) => actions.switchScenario(e.target.value as any)}>{SCENARIO_OPTIONS.map((s) => <option key={s.id} value={s.id}>{s.name}</option>)}</select></Card><Card title="Evidence chain"><ul>{state.evidence.map((e) => <li key={e.key}>{e.label}: {e.status}</li>)}</ul></Card></div></main>; }
