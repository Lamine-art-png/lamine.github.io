import { useSyncExternalStore } from "react";
import { apiClient } from "../api/client";
import { probeBackend } from "../api/health";
import type { AnalysisMode, BackendStatus, Decision, EvidenceStep, ReconciliationRow, RecommendationOrigin, SourceRow, TraceStep, WorkbenchAnalysisResult } from "../api/contracts";

export type ScenarioId = "alpha-vineyard" | "almond-orchard" | "multi-farm" | "partner-validation";
export type Route = "command" | "sources" | "reports" | "integrations" | "audit" | "settings" | "compliance";
export interface ReportModel { farm: string; block: string; recommendation: string; plannedWater: string; appliedWater: string; variance: string; evidenceCompleteness: string; estimatedWaterSavings: string; verification: string; }
export interface UploadedFileState { name: string; detectedType: string; parseStatus: string; rows: string; fields: string; warnings: string; }
export interface AuditEvent { time: string; actor: string; event: string; detail: string; }
export interface CommandState { route: Route; backend: { status: BackendStatus; detail: string; checkedAt: string }; scenarioId: ScenarioId; analysisMode: AnalysisMode; analysisPhase: "idle" | "running" | "complete" | "error"; pipelineMessage: string; recommendationOrigin: RecommendationOrigin; decision: Decision; sources: SourceRow[]; reconciliation: ReconciliationRow[]; trace: TraceStep[]; evidence: EvidenceStep[]; report: ReportModel; toast: string | null; drawerOpen: boolean; uploaded: UploadedFileState | null; audit: AuditEvent[]; }

interface Scenario { id: ScenarioId; name: string; liveEntity: { source: string; entityId: string }; decision: Omit<Decision, "recommendationOrigin">; sources: SourceRow[]; reconciliation: ReconciliationRow[]; report: ReportModel; }
const REP_SOURCES_ALPHA: SourceRow[] = [
  { source: "Controller history", latestSignal: "Last irrigation event: 36 min", records: "1,248", contribution: "+0.22", status: "Matched" },
  { source: "Weather demand", latestSignal: "ETo 6.4 mm, rain 0 mm", records: "336", contribution: "+0.18", status: "Matched" },
  { source: "Soil moisture", latestSignal: "38% deficit at 30 cm", records: "412", contribution: "+0.17", status: "Matched" },
  { source: "Flow meter", latestSignal: "Actual flow within 8% of plan", records: "298", contribution: "+0.11", status: "Matched" },
  { source: "Field observation", latestSignal: "Mild afternoon stress", records: "26", contribution: "+0.09", status: "Matched" },
  { source: "Earth observation layer", latestSignal: "Elevated canopy stress index", records: "84", contribution: "+0.09", status: "Matched" },
  { source: "Uploaded records", latestSignal: "Awaiting CSV, XLSX, JSON, or TXT", records: "0", contribution: "Pending", status: "Pending" },
];
function recon(rows: [string, string, string, ReconciliationRow["status"]][]): ReconciliationRow[] { return rows.map(([source, signal, interpretation, status]) => ({ source, signal, interpretation, status })); }
const SCENARIOS: Record<ScenarioId, Scenario> = {
  "alpha-vineyard": { id: "alpha-vineyard", name: "Alpha Vineyard", liveEntity: { source: "wiseconn", entityId: "162803" }, decision: { action: "Irrigate 42 min tonight", start: "21:00 PT", appliedWater: "12 mm net", crop: "Cabernet Sauvignon", block: "Block A North", driver: "ETo 6.4 mm and 38% root-zone deficit", confidence: "86%", evidenceCompleteness: "92%", estimatedWaterSavings: "27%", verification: "Required" }, sources: REP_SOURCES_ALPHA, reconciliation: recon([["Controller history", "Last irrigation event: 36 min", "Valid recent controller event", "Matched"], ["Weather demand", "ETo 6.4 mm, rain 0 mm", "High water demand", "Matched"], ["Soil moisture", "38% deficit at 30 cm", "Root-zone deficit supports irrigation", "Matched"], ["Flow meter", "Actual flow within 8% of plan", "Applied water consistent", "Matched"], ["Field observation", "Mild afternoon stress", "Supports irrigation recommendation", "Matched"], ["Earth observation layer", "Elevated canopy stress index", "Supports water demand signal", "Matched"], ["Talgil", "Runtime reachable, no selected production target", "Integration available, target selection pending", "Pending target"]]), report: { farm: "Alpha Vineyard", block: "Block A North", recommendation: "Irrigate 42 min tonight", plannedWater: "12 mm net", appliedWater: "Pending confirmation", variance: "Within 8%", evidenceCompleteness: "92%", estimatedWaterSavings: "27% vs historical baseline", verification: "Required" } },
  "almond-orchard": { id: "almond-orchard", name: "Almond Orchard", liveEntity: { source: "wiseconn", entityId: "204411" }, decision: { action: "Apply 18 mm before 05:00", start: "03:15 local", appliedWater: "18 mm net", crop: "Almonds", block: "Almond Block 4", driver: "ETo 6.5 mm and 42% root-zone deficit", confidence: "91%", evidenceCompleteness: "94%", estimatedWaterSavings: "31%", verification: "Required" }, sources: [{ source: "Controller history", latestSignal: "Last set 55 min, mild applied variance", records: "980", contribution: "+0.21", status: "Matched" }, { source: "Weather demand", latestSignal: "ETo 6.5 mm, rain 0 mm", records: "288", contribution: "+0.19", status: "Matched" }, { source: "Soil moisture", latestSignal: "42% deficit at 30 cm", records: "366", contribution: "+0.18", status: "Matched" }, { source: "Flow meter", latestSignal: "Prior set +12.3% over plan", records: "212", contribution: "+0.08", status: "Review" }, { source: "Field observation", latestSignal: "Mild leaf curl, southwest corner", records: "18", contribution: "+0.08", status: "Matched" }, { source: "Earth observation layer", latestSignal: "Vegetation stress index 0.52", records: "60", contribution: "+0.10", status: "Matched" }, { source: "Uploaded records", latestSignal: "Awaiting CSV, XLSX, JSON, or TXT", records: "0", contribution: "Pending", status: "Pending" }], reconciliation: recon([["Controller history", "Last set 55 min, mild applied variance", "Valid controller event", "Matched"], ["Weather demand", "ETo 6.5 mm, rain 0 mm", "High water demand", "Matched"], ["Soil moisture", "42% deficit at 30 cm", "Root-zone deficit supports irrigation", "Matched"], ["Flow meter", "Prior set +12.3% over plan", "Applied-water variance flagged", "Review"], ["Field observation", "Mild leaf curl at southwest corner", "Supports irrigation recommendation", "Matched"], ["Earth observation layer", "Vegetation stress index 0.52", "Supports water demand signal", "Matched"], ["Talgil", "Not used for this orchard", "WiseConn-managed orchard", "Pending target"]]), report: { farm: "Delta Almonds", block: "Almond Block 4", recommendation: "Apply 18 mm before 05:00", plannedWater: "18 mm net", appliedWater: "Pending confirmation", variance: "Prior set +12.3%", evidenceCompleteness: "94%", estimatedWaterSavings: "31% vs historical baseline", verification: "Required" } },
  "multi-farm": { id: "multi-farm", name: "Multi-Farm Portfolio", liveEntity: { source: "wiseconn", entityId: "162803" }, decision: { action: "3 blocks irrigate tonight, 1 hold", start: "Tonight, staggered windows", appliedWater: "12–18 mm net", crop: "Mixed (vineyard + almond)", block: "4 active blocks", driver: "Portfolio root-zone deficit across 3 of 4 blocks", confidence: "88%", evidenceCompleteness: "90%", estimatedWaterSavings: "26%", verification: "Required" }, sources: REP_SOURCES_ALPHA.map((row) => ({ ...row, records: row.records === "1,248" ? "3,140" : row.records })), reconciliation: recon([["Controller history", "12 controller events across 3 farms", "Recent events validated", "Matched"], ["Weather demand", "ETo 6.0–6.8 mm by region", "High demand in 3 of 4 blocks", "Matched"], ["Soil moisture", "Deficit 28–44% by block", "Mixed deficit, one block adequate", "Matched"], ["Flow meter", "One block over plan, others in range", "Applied-water variance localized", "Review"], ["Field observation", "Stress notes on 2 blocks", "Supports irrigation in those blocks", "Matched"], ["Earth observation layer", "Elevated stress on vineyard blocks", "Supports water demand signal", "Matched"], ["Talgil", "North Ridge runtime reachable", "Target selection pending", "Pending target"]]), report: { farm: "Portfolio (3 farms)", block: "4 active blocks", recommendation: "3 blocks irrigate tonight, 1 hold", plannedWater: "12–18 mm net", appliedWater: "Pending confirmation", variance: "Localized to 1 block", evidenceCompleteness: "90%", estimatedWaterSavings: "26% vs historical baseline", verification: "Required" } },
  "partner-validation": { id: "partner-validation", name: "Partner Data Validation", liveEntity: { source: "talgil", entityId: "trial-11" }, decision: { action: "Validate partner feed before scheduling", start: "Pending partner feed authorization", appliedWater: "Awaiting validation", crop: "Trial vineyard", block: "Vineyard Block Trial", driver: "Partner feed ingested as representative data; pressure coverage partial", confidence: "73%", evidenceCompleteness: "78%", estimatedWaterSavings: "—", verification: "Partner feed authorization required for production use" }, sources: [{ source: "Controller history", latestSignal: "Talgil trial rows ingested", records: "210", contribution: "+0.14", status: "Matched" }, { source: "Weather demand", latestSignal: "ETo within seasonal range", records: "120", contribution: "+0.12", status: "Matched" }, { source: "Soil moisture", latestSignal: "Partial sensor coverage", records: "64", contribution: "+0.06", status: "Review" }, { source: "Flow meter", latestSignal: "Prior set variance +23%", records: "40", contribution: "Review", status: "Review" }, { source: "Field observation", latestSignal: "Trial rows watched separately", records: "12", contribution: "+0.05", status: "Matched" }, { source: "Earth observation layer", latestSignal: "Partner-provided sample layer", records: "30", contribution: "Pending", status: "Pending target" }, { source: "Uploaded records", latestSignal: "Awaiting CSV, XLSX, JSON, or TXT", records: "0", contribution: "Pending", status: "Pending" }], reconciliation: recon([["Controller history", "Talgil trial rows ingested", "Trial block, separated from commercial", "Matched"], ["Weather demand", "ETo within seasonal range", "Demand evaluated", "Matched"], ["Soil moisture", "Partial sensor coverage", "Coverage gap flagged", "Review"], ["Flow meter", "Prior set variance +23%", "Applied-water variance flagged", "Review"], ["Field observation", "Trial rows watched separately", "Supports cautious recommendation", "Matched"], ["Earth observation layer", "Partner-provided sample layer", "Representative until authorized", "Pending target"], ["Talgil", "Runtime reachable, trial target", "Production authorization required", "Pending target"]]), report: { farm: "West Citrus", block: "Vineyard Block Trial", recommendation: "Validate partner feed before scheduling", plannedWater: "Awaiting validation", appliedWater: "Awaiting validation", variance: "Partner feed unverified", evidenceCompleteness: "78%", estimatedWaterSavings: "—", verification: "Partner feed authorization required for production use" } },
};
export const SCENARIO_OPTIONS = Object.values(SCENARIOS).map((s) => ({ id: s.id, name: s.name }));
const TRACE_TITLES = ["Source records ingested", "Schema detected", "Units normalized", "Field context assembled", "Source conflicts reconciled", "Confidence scored", "Recommendation prepared", "Verification plan prepared"];
function buildTrace(now: string, complete: boolean): TraceStep[] { return TRACE_TITLES.map((title, i) => ({ title, status: complete ? "complete" : "pending", recordsProcessed: [2404, 8, 2404, 1, 7, 1, 1, 5][i], detail: `${title} for the active water decision package.`, timestamp: complete ? now : "—" })); }
function baseEvidence(now: string): EvidenceStep[] { const steps: Array<[EvidenceStep["key"], string, string]> = [["recommended", "Recommended", "AGRO-AI Intelligence Engine"], ["scheduled", "Scheduled", "Operations user"], ["applied", "Applied", "Operations user"], ["observed", "Observed", "Operations user"], ["verified", "Verified", "AGRO-AI Verification"]]; return steps.map(([key, label, owner], i) => ({ key, label, owner, status: i === 0 ? "Complete" : "Pending", timestamp: i === 0 ? now : "", evidence: i === 0 ? "Verified water decision produced from current source set." : `${label} pending` })); }
function scenarioState(id: ScenarioId, mode: AnalysisMode, origin: RecommendationOrigin): Partial<CommandState> { const sc = SCENARIOS[id]; const now = new Date().toISOString(); return { scenarioId: id, analysisMode: mode, analysisPhase: "complete", recommendationOrigin: origin, pipelineMessage: "Decision refreshed", decision: { ...sc.decision, recommendationOrigin: origin }, sources: sc.sources, reconciliation: sc.reconciliation, trace: buildTrace(now, true), evidence: baseEvidence(now), report: sc.report }; }
function initialState(): CommandState { const seeded = scenarioState("alpha-vineyard", "representative", "representative_fallback"); return { ...seeded, route: "command", backend: { status: "limited", detail: "Checking backend…", checkedAt: "" }, toast: null, drawerOpen: false, uploaded: null, audit: [{ time: new Date().toISOString(), actor: "Operations user", event: "Workspace launched", detail: "Representative package loaded." }] } as CommandState; }
let state = initialState();
const listeners = new Set<() => void>();
function emit() { for (const listener of listeners) listener(); }
function set(patch: Partial<CommandState>) { state = { ...state, ...patch }; emit(); }
function addAudit(event: string, detail: string) { set({ audit: [{ time: new Date().toISOString(), actor: "Operations user", event, detail }, ...state.audit].slice(0, 60) }); }
function toast(message: string) { set({ toast: message }); setTimeout(() => { if (state.toast === message) set({ toast: null }); }, 3200); }
function pct(value: unknown, fallback: string) { if (typeof value === "number") return value <= 1 ? `${Math.round(value * 100)}%` : `${Math.round(value)}%`; if (typeof value === "string" && value.trim()) return value; return fallback; }
function applyBackendResult(result: WorkbenchAnalysisResult, mode: AnalysisMode) { const sc = SCENARIOS[state.scenarioId]; const rec = result.recommendation ?? {}; const summary = result.report_summary ?? {}; const origin = result.recommendation_origin ?? (mode === "live" ? "live_intelligence_engine" : "deterministic_engine"); const now = new Date().toISOString(); const action = String(rec.action || rec.decision || summary.recommendation || sc.decision.action); const decision: Decision = { action, start: String(rec.start_time || rec.timing || sc.decision.start), appliedWater: String(rec.depth || summary.planned_water || sc.decision.appliedWater), crop: String(rec.crop || sc.decision.crop), block: String(rec.block || sc.decision.block), driver: Array.isArray(rec.key_drivers) && rec.key_drivers.length ? String(rec.key_drivers[0]) : sc.decision.driver, confidence: pct(rec.confidence ?? summary.confidence, sc.decision.confidence), evidenceCompleteness: pct(result.reconciliation?.evidence_completeness ?? summary.evidence_completeness, sc.decision.evidenceCompleteness), estimatedWaterSavings: sc.decision.estimatedWaterSavings, verification: String(rec.verification_requirement || sc.decision.verification), recommendationOrigin: origin }; const trace = result.analysis_trace?.length ? result.analysis_trace.map((t) => ({ title: t.title, status: t.status === "review" ? "review" as const : "complete" as const, recordsProcessed: t.objects_processed ?? 0, detail: t.details ?? "", timestamp: now })) : buildTrace(now, true); set({ analysisMode: mode, analysisPhase: "complete", recommendationOrigin: origin, pipelineMessage: "Decision refreshed", decision, trace, evidence: baseEvidence(now), report: { ...sc.report, recommendation: action, evidenceCompleteness: decision.evidenceCompleteness } }); }
export const actions = { async init() { const health = await probeBackend(); set({ backend: health }); }, navigate(route: Route) { set({ route }); }, switchScenario(id: ScenarioId) { set(scenarioState(id, "representative", "representative_fallback")); addAudit("Workspace scenario loaded", `${SCENARIOS[id].name} representative records loaded.`); toast(`${SCENARIOS[id].name} loaded`); }, async refreshIntelligence() { if (state.analysisPhase === "running") return; set({ analysisPhase: "running", pipelineMessage: "Analyzing source records…", trace: buildTrace(new Date().toISOString(), false) }); addAudit("Intelligence analysis started", `Mode: ${state.analysisMode}.`); let result: WorkbenchAnalysisResult | null = null; if (state.backend.status !== "unavailable") { try { const sc = SCENARIOS[state.scenarioId]; const res = await apiClient.analyzeLive(sc.liveEntity.source, sc.liveEntity.entityId); if (res.ok && res.data) result = res.data; } catch { result = null; } } await new Promise((r) => setTimeout(r, 200)); if (result) { applyBackendResult(result, "live"); addAudit("Intelligence analysis completed", "Live Workbench result applied."); toast("Analysis complete. Decision ready."); } else { set(scenarioState(state.scenarioId, "representative", "representative_fallback")); set({ pipelineMessage: "Backend unavailable. Representative analysis remains active." }); addAudit("Intelligence analysis completed", "Representative-data analysis applied."); toast("Representative analysis refreshed."); } }, async uploadRecords(file: File) { set({ pipelineMessage: "Analyzing source records…", analysisPhase: "running", uploaded: { name: file.name, detectedType: detectType(file.name), parseStatus: "Uploading…", rows: "—", fields: "—", warnings: "—" } }); let sessionId = ""; const created = await apiClient.createSession("uploaded"); if (created.ok) sessionId = created.data?.session_id || created.data?.session?.session_id || ""; if (!sessionId) { set({ analysisPhase: "complete", pipelineMessage: "Backend unavailable. Representative analysis remains active.", uploaded: { name: file.name, detectedType: detectType(file.name), parseStatus: "Backend upload endpoint unavailable", rows: "—", fields: "—", warnings: "Representative analysis remains active." } }); toast("Backend upload unavailable. Representative analysis remains active."); return; } const up = await apiClient.uploadFile(sessionId, file); if (up.ok && up.data) { const a = up.data; set({ uploaded: { name: a.filename || file.name, detectedType: a.source_kind || detectType(file.name), parseStatus: a.parse_status || "parsed", rows: String(a.rows_detected ?? "—"), fields: Array.isArray(a.columns_detected) ? String(a.columns_detected.length) : "—", warnings: a.warnings?.length ? a.warnings.join("; ") : "None" } }); const analysis = await apiClient.analyzeSession(sessionId); if (analysis.ok && analysis.data) { applyBackendResult(analysis.data, "uploaded"); addAudit("Uploaded records analyzed", `${file.name} analyzed via Workbench.`); toast("Uploaded records analyzed."); return; } } set({ analysisPhase: "complete", pipelineMessage: "Backend unavailable. Representative analysis remains active." }); toast("Upload analysis unavailable. Representative analysis remains active."); }, openDrawer() { set({ drawerOpen: true }); }, closeDrawer() { set({ drawerOpen: false }); }, advanceEvidence(key: EvidenceStep["key"]) { const order: EvidenceStep["key"][] = ["recommended", "scheduled", "applied", "observed", "verified"]; const idx = order.indexOf(key); const now = new Date().toISOString(); const evidenceText: Record<EvidenceStep["key"], string> = { recommended: "Verified water decision produced from current source set.", scheduled: "Irrigation scheduled in the controller window.", applied: "Applied water confirmed from controller event.", observed: "Field team recorded canopy response after irrigation.", verified: "Outcome verified against controller event and observation." }; const evidence = state.evidence.map((step, i) => i <= idx ? { ...step, status: "Complete" as const, timestamp: step.timestamp || now, evidence: evidenceText[step.key] } : step); set({ evidence }); addAudit(`${order[idx]} recorded`, evidenceText[key]); toast(`${state.evidence[idx]?.label ?? "Step"} recorded`); }, dismissToast() { set({ toast: null }); } };
function detectType(filename: string) { const ext = filename.split(".").pop()?.toLowerCase(); const map: Record<string, string> = { csv: "Tabular records (CSV)", xlsx: "Spreadsheet export (XLSX)", json: "Structured records (JSON)", txt: "Field notes (TXT)" }; return (ext && map[ext]) || "Detected on upload"; }
export function useCommandStore<T>(selector: (s: CommandState) => T): T { return useSyncExternalStore((cb: () => void) => { listeners.add(cb); return () => listeners.delete(cb); }, () => selector(state), () => selector(state)); }
export function getState(): CommandState { return state; }
export function __resetForTest() { state = initialState(); emit(); }
