# Portal language switching release candidate

This branch makes locale activation atomic, turns static portal literals into a generated source inventory, makes mounted UI locale-reactive, generates large catalogs in validated chunks, and tests every visible non-English locale in the browser contract.
