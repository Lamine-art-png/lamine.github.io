# ADR 010: Physical Action Safety

Decision: Physical irrigation command execution is disabled in the private-beta foundation.

AI output may plan actions only. Execution requires future provider write contracts, explicit scopes, live environment, entitlement, deterministic safety checks, fresh state, idempotency, customer approval, audit, acknowledgement, reconciliation, and kill switches.
