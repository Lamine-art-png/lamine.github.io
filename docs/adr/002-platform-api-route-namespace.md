# ADR 002: Platform API Route Namespace

Decision: New partner and developer routes use `/v1/platform/...`.

Existing Portal URLs remain unchanged. Internal routes stay under `/v1/internal/...`; platform-admin routes remain separate.
