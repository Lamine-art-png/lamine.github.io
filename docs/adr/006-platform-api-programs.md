# ADR 006: one Platform API with program enrollments

Accepted.

Use one organization-scoped project/key/usage/webhook platform. Durable program
enrollments apply access policy and commercial differences. Separate stacks
would drift in authentication, isolation, metering, and incident controls.
Enrollment never overrides organization verification.
