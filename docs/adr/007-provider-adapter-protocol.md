# ADR 007: Provider Adapter Protocol

Decision: Strategic providers are implementation objects in a registry, not display-only catalog metadata.

EarthDaily and Valley start as readiness adapters. They do not claim live authentication, schemas, rate limits, or provider calls.
