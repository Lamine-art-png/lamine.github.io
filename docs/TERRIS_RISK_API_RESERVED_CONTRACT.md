# Terris Risk API Reserved Contract

Terris Risk API is reserved for a future institutional contract boundary.

This phase adds no public endpoint, no fabricated portfolio metrics, and no backend persistence. Any future Risk API must define tenant-scoped inputs, evidence references, authorization, retention, explainability, and export controls before implementation.

The mobile registry may show the module as reserved, but it remains disabled by default.
