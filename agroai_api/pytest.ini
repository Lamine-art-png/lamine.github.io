[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    -p tests.unit.connector_v3_db_plugin
markers =
    unit: Unit tests
    integration: Integration tests
    acceptance: Acceptance tests
